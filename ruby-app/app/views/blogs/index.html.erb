<div class="max-w-6xl mx-auto px-4 py-16">
  <h1 class="text-4xl font-bold mb-8 text-gray-900">All Blog Posts</h1>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
    <% @posts.each do |post| %>
      <%= render "shared/blog_card", post: post %>
    <% end %>
  </div>
</div>
