<header class="bg-white shadow-sm sticky top-0 z-50">
  <nav class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
    <%= render "shared/logo" %>
    <div class="flex gap-6">
      <%= link_to "Home", root_path, class: "hover:text-blue-600 transition-colors font-medium #{'text-blue-600' if current_page?(root_path)}" %>
      <%= link_to "Blog", blog_path, class: "hover:text-blue-600 transition-colors font-medium #{'text-blue-600' if current_page?(blog_path) || controller_name == 'blogs'}" %>
    </div>
  </nav>
</header>
